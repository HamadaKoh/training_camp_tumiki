# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=loadtest
DB_PASSWORD=password
DB_NAME=loadtest_db

# Backend Configuration
BACKEND_PORT=3000
NODE_ENV=development

# Frontend Configuration
FRONTEND_PORT=5173

# GKE Configuration (will be configured in TASK-003)
GKE_PROJECT_ID=
GKE_CLUSTER_NAME=
GKE_CLUSTER_ZONE=
GKE_SERVICE_ACCOUNT=

# Application Configuration
LOAD_TARGET_NAMESPACE=default
LOAD_TARGET_DEPLOYMENT=load-target-nginx
HPA_NAME=load-target-hpa

# Monitoring Configuration
METRICS_POLL_INTERVAL=5000
CACHE_TTL=1000